{
  "name": "ugonfor-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-html.mjs && vite build && node scripts/verify-build.mjs",
    "dev": "npm run build && npx serve dist -l 4000",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7",
    "vite": "^7.3.1"
  }
}
