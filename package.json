{
  "name": "ugonfor-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-html.mjs",
    "dev": "npm run build && npx serve dist -l 4000"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  }
}
