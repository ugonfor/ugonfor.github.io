# Copy this file to server/.env and fill in real values.
# Do not commit server/.env

GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY
MODEL_CHAIN=gemini-2.0-flash,gemini-2.5-pro,gemma-3-27b-it,gemma-3-12b-it
ALLOWED_ORIGINS=http://localhost:4000,http://127.0.0.1:4000,https://ugonfor.kr,https://www.ugonfor.kr
# Optional: if set, every API request must include matching X-Proxy-Token header.
# PROXY_AUTH_TOKEN=replace-with-long-random-secret
# Optional: if set, POST requests to LLM/NPC APIs must include valid X-Turnstile-Token.
# TURNSTILE_SECRET_KEY=replace-with-turnstile-secret
# Optional override:
# TURNSTILE_VERIFY_URL=https://challenges.cloudflare.com/turnstile/v0/siteverify
# Optional hostname check (comma-separated):
# TURNSTILE_EXPECTED_HOSTNAMES=ugonfor.kr,www.ugonfor.kr
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=90
MAX_BODY_BYTES=1000000

# Local run defaults
PORT=8787
HOST=127.0.0.1
